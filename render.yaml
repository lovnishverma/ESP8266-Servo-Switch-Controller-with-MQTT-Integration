services:
- type: web
  name: esp8266-servo-switch-controller-with
  env: python
  plan: free
  buildCommand: pip install -r requirements.txt
  startCommand: gunicorn app:app --worker-class eventlet -w 1 -b 0.0.0.0:10000
  envVars:
    - key: PORT
      value: 10000
